import { CardsPageComponent } from './cards-page.component';

describe('CardsPageComponent', () => {
  let component: CardsPageComponent;

  beforeEach(() => {
    component = new CardsPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
