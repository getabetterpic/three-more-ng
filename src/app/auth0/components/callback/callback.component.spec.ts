import { CallbackComponent } from './callback.component';

describe('CallbackComponent', () => {
  let component: CallbackComponent;

  beforeEach(() => {
    component = new CallbackComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
