import { CardTextTransformPipe } from './card-text-transform.pipe';

describe('CardTextTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CardTextTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
